{
  "postgresql": {
    "host": "${DB_HOST}",
    "port": "${DB_PORT}",
    "url": "${DB_URL}",
    "database": "${DB_NAME}",
    "password": "${DB_PASSWORD}",
    "name": "postgresql",
    "user": "${DB_USER}",
    "connector": "postgresql"
  }
}

